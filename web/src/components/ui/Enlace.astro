---
interface Props {
  href: string
  target?: string
  class?: string
  title?: string
}

const {
  href,
  target = '_self',
  class: className,
  title
} = Astro.props
---

<a href={href} target={target} class={className} title={title}>
  <slot />
</a>
